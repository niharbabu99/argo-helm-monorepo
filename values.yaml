build-step-chart:
  enabled: false

security-check-chart:
  enabled: false

image-creation-chart:
  enabled: false

build:
    parameters:
      git_repo_name: sample-application
      git_revision: main

build_step:
  chartVersion: 0.3.0
